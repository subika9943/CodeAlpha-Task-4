Question:

Build a simple rule-based chatbot that:

Takes user input such as "hello", "how are you", "bye"

Responds with predefined replies like "Hi!", "I'm fine, thanks!", "Goodbye!"

Uses basic Python concepts

Reference:

This program uses the following Python concepts:

if–elif–else → To match user input with predefined responses

functions → To organize chatbot logic

loops (while) → To keep the chatbot running

input() / print() → For user interaction

string methods → .lower() to handle case-insensitive input

Tools :

Programming Language: Python

Editor/IDE: Any (IDLE, VS Code, PyCharm, Jupyter, etc.)

Platform: Command Line / Terminal
